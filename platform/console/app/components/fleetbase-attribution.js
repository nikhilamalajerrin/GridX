import templateOnly from '@ember/component/template-only';

// Shadows the @fleetbase/ember-ui re-export so the co-located
// GridX template in this directory is used instead.
export default templateOnly();
