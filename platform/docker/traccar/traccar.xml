<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE properties SYSTEM 'http://java.sun.com/dtd/properties.dtd'>
<properties>
    <entry key='config.default'>./conf/default.xml</entry>

    <!-- Embedded H2 database (persisted via ./docker/traccar/data volume) -->
    <entry key='database.driver'>org.h2.Driver</entry>
    <entry key='database.url'>jdbc:h2:./data/database</entry>
    <entry key='database.user'>sa</entry>
    <entry key='database.password'></entry>

    <!-- Forward every position to the GridX agent, which relays it
         to the GridX API (driver location on the Fleet-Ops map). -->
    <entry key='forward.enable'>true</entry>
    <entry key='forward.url'>http://host.docker.internal:9000/track/traccar</entry>
    <entry key='forward.type'>json</entry>
    <entry key='forward.retry.enable'>true</entry>
</properties>
